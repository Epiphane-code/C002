#include <stdio.h>
int main()
{   // déclaration de variable 
    int n;
    scanf("%d",&n);
    // vérifier si le nombre est divisible par deux 
    // si oui, le nombre est impair, sinon impair
    if (n%2==0)
     printf("%d est pair.\n", n);
     else
     printf("%d est impair.\n", n);
    
}
