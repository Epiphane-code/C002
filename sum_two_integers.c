#include <stdio.h>
int main()
{   //déclaration des variables 
    int nb1, nb2, somme;
    // lire les valeurs qu'on va additionner
    scanf("%d %d", &nb1, &nb2);
    // affiche la somme de ces nombres
    printf("La somme est : %d\n", nb1 + nb2);
}

